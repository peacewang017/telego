# pnpm配置文件
# 严格的peer依赖处理
strict-peer-dependencies=false
auto-install-peers=true

# 使用符号链接而不是硬链接 (适用于某些文件系统)
# symlink=true

# 安装时显示进度
progress=true

# 在CI环境中自动使用 --frozen-lockfile (注释掉，避免首次安装时报错)
# frozen-lockfile=true

# 设置registry (如果需要使用国内镜像)
# registry=https://registry.npmmirror.com/

# 设置node_modules结构为扁平化 (如果需要兼容某些工具)
# node-linker=hoisted 